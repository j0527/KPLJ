class SliderlItems {
  final String imagePath; // 슬라이더의 이미지 정보
  final String description; // 슬라이더의 텍스트 정보(유저 정보를 담을 곳으로 더 추가해야함)

  SliderlItems({
    required this.imagePath,
    required this.description,
  });
}
